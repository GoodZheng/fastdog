<Window x:Class="FastDog.UpdateProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="正在下载更新"
        Width="440" Height="180"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Icon="Assets/newLogo1.2.ico"
        UseLayoutRounding="True"
        Background="#f3f3f3"
        Loaded="Window_Loaded">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="36" GlassFrameThickness="-1"
                            ResizeBorderThickness="0" CornerRadius="0"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Window.Template>
        <ControlTemplate TargetType="Window">
            <Border Background="{TemplateBinding Background}">
                <ContentPresenter/>
            </Border>
        </ControlTemplate>
    </Window.Template>

    <DockPanel>
        <!-- ===== 标题栏（与主窗口一致） ===== -->
        <Border DockPanel.Dock="Top" Height="36" Background="#faf8f3"
                BorderBrush="#ebe6da" BorderThickness="0,0,0,1">
            <Grid>
                <!-- 左：图标 + 标题 -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center"
                            Margin="12,0,0,0">
                    <Image Source="Assets/newLogo1.2.ico" Width="22" Height="22"
                           Margin="0,0,8,0"
                           RenderOptions.BitmapScalingMode="HighQuality"/>
                    <TextBlock VerticalAlignment="Center">
                        <Run Text="Fast" FontWeight="ExtraBold" FontSize="13" Foreground="#e74c3c"/>
                        <Run Text="Dog" FontWeight="ExtraBold" FontSize="13" Foreground="#1e1e1e"/>
                        <Run Text=" — 正在下载更新" FontSize="12" Foreground="#9a9a9a"/>
                    </TextBlock>
                </StackPanel>
                <!-- 右：关闭按钮 -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="TitleCloseButton"
                            Style="{StaticResource CaptionCloseButton}"
                            Click="TitleCloseButton_Click"
                            ToolTip="关闭"
                            shell:WindowChrome.IsHitTestVisibleInChrome="True">
                        <Path Width="11" Height="11" Stroke="#1e1e1e" StrokeThickness="1.1"
                              Data="M1,1 L10,10 M10,1 L1,10"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ===== 内容区 ===== -->
        <Grid Margin="20,16,20,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="StatusText" Grid.Row="0"
                       Text="正在连接服务器…"
                       FontSize="13" Foreground="#1e1e1e"/>

            <ProgressBar x:Name="DownloadProgress" Grid.Row="1"
                         Margin="0,10,0,0" Height="16"
                         Minimum="0" Maximum="100" Value="0"
                         Foreground="#0e639c" Background="#e0e0e0"/>

            <TextBlock x:Name="ProgressDetail" Grid.Row="2"
                       Margin="0,6,0,0"
                       FontSize="12" Foreground="#9a9a9a"/>

            <Button Grid.Row="4" Content="取消"
                    HorizontalAlignment="Right"
                    Width="72" Height="28"
                    Click="CancelButton_Click"
                    Style="{StaticResource CancelButton}"/>
        </Grid>
    </DockPanel>
</Window>
