<Window x:Class="FastDog.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:models="clr-namespace:FastDog.Models"
        Title="FastDog — 基于 ripgrep 的文本搜索" Height="800" Width="1200"
        Icon="Assets/fastdog.ico"
        WindowStartupLocation="CenterScreen"
        AllowDrop="True" DragOver="Window_DragOver" Drop="Window_Drop"
        MouseDown="Window_MouseDown"
        StateChanged="Window_StateChanged"
        DataContext="{StaticResource MainViewModel}"
        Background="#f3f3f3"
        UseLayoutRounding="True">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="36" GlassFrameThickness="0"
                            ResizeBorderThickness="6" CornerRadius="0"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Window.Template>
        <ControlTemplate TargetType="Window">
            <Border Background="{TemplateBinding Background}"
                    BorderBrush="#cccccc" BorderThickness="1">
                <ContentPresenter/>
            </Border>
        </ControlTemplate>
    </Window.Template>
    <DockPanel>

        <!-- ==================== 自定义标题栏 ==================== -->
        <Border DockPanel.Dock="Top" Height="36" Background="White"
                BorderBrush="#e0e0e0" BorderThickness="0,0,0,1">
            <Grid>
                <!-- 左侧：图标 + 彩色 Logo + 版本 -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
                    <Border Width="16" Height="16" CornerRadius="4" Margin="0,0,8,0">
                        <Border.Background>
                            <LinearGradientBrush EndPoint="1,1">
                                <GradientStop Color="#0e639c" Offset="0"/>
                                <GradientStop Color="#2d7fb8" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <TextBlock Text="F" Foreground="White" FontWeight="Bold" FontSize="10"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock VerticalAlignment="Center">
                        <Run Text="Fast" FontWeight="ExtraBold" FontSize="13" Foreground="#e74c3c"/>
                        <Run Text="Dog" FontWeight="ExtraBold" FontSize="13" Foreground="#1e1e1e"/>
                        <Run Text="v1.1.0 — 基于 ripgrep 的文本搜索" FontSize="12" Foreground="#9a9a9a"/>
                    </TextBlock>
                </StackPanel>

                <!-- 右侧：窗口控制按钮 -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="MinimizeButton" Style="{StaticResource CaptionButton}"
                            Click="MinimizeButton_Click" ToolTip="最小化"
                            shell:WindowChrome.IsHitTestVisibleInChrome="True">
                        <Path Width="11" Height="11" Stroke="#1e1e1e" StrokeThickness="1"
                              Data="M0,5.5 L11,5.5"/>
                    </Button>
                    <Button x:Name="MaximizeButton" Style="{StaticResource CaptionButton}"
                            Click="MaximizeButton_Click" ToolTip="最大化"
                            shell:WindowChrome.IsHitTestVisibleInChrome="True">
                        <Path x:Name="MaximizeIcon" Width="11" Height="11"
                              Stroke="#1e1e1e" StrokeThickness="1"
                              Data="M1,1 L10,1 L10,10 L1,10 Z"/>
                    </Button>
                    <Button x:Name="CloseButton" Style="{StaticResource CaptionCloseButton}"
                            Click="CloseButton_Click" ToolTip="关闭"
                            shell:WindowChrome.IsHitTestVisibleInChrome="True">
                        <Path Width="11" Height="11" Stroke="#1e1e1e" StrokeThickness="1.1"
                              Data="M1,1 L10,10 M10,1 L1,10"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ==================== Header: 搜索条件区 ==================== -->
        <Border DockPanel.Dock="Top" Background="#fff"
                BorderBrush="#e0e0e0" BorderThickness="0,0,0,1"
                Padding="16,14,16,10">
            <StackPanel>
                <!-- 搜索路径 -->
                <DockPanel Margin="0,0,0,6">
                    <TextBlock Text="搜索路径" FontSize="12" Foreground="#9a9a9a"
                               Width="55" VerticalAlignment="Center"/>
                    <Button DockPanel.Dock="Right" Content="浏览..."
                            Command="{Binding BrowseCommand}"
                            Style="{StaticResource ActionButton}" Margin="6,0,0,0"/>
                    <TextBox Text="{Binding SearchPath, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource SearchInput}"/>
                </DockPanel>

                <!-- 搜索内容 -->
                <DockPanel Margin="0,0,0,8">
                    <TextBlock Text="搜索内容" FontSize="12" Foreground="#9a9a9a"
                               Width="55" VerticalAlignment="Center"/>
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource SearchInput}"/>
                </DockPanel>

                <!-- 按钮行 -->
                <WrapPanel Orientation="Horizontal">
                    <!-- 搜索模式 -->
                    <RadioButton GroupName="SearchMode"
                                 Style="{StaticResource ToggleOptionButton}"
                                 IsChecked="{Binding IsRegex}"
                                 Content="正则表达式"/>
                    <RadioButton GroupName="SearchMode"
                                 Style="{StaticResource ToggleOptionButton}"
                                 IsChecked="{Binding IsPlainText}"
                                 Content="纯文本"/>

                    <!-- 选项 -->
                    <CheckBox Style="{StaticResource ToggleOptionButton}"
                              IsChecked="{Binding CaseSensitive}"
                              Content="区分大小写 (Aa)"/>
                    <CheckBox Style="{StaticResource ToggleOptionButton}"
                              IsChecked="{Binding WholeWord}"
                              Content="全词匹配"/>

                    <!-- 分隔线 -->
                    <TextBlock Text="|" Foreground="#e0e0e0" Margin="4,0,4,0"
                               VerticalAlignment="Center" FontSize="12"/>

                    <!-- 文件过滤 -->
                    <Grid Width="120" Margin="0,0,4,0">
                        <Button x:Name="FileFilterButton"
                                Style="{StaticResource TagButton}"
                                Content="{Binding FileFilterDisplay}"
                                Click="FileFilterButton_Click"
                                HorizontalAlignment="Stretch"/>
                        <TextBox x:Name="FileFilterTextBox"
                                 Style="{StaticResource TagInput}"
                                 Text="{Binding FileFilter, UpdateSourceTrigger=PropertyChanged}"
                                 Visibility="Collapsed"
                                 LostFocus="FileFilterTextBox_LostFocus"
                                 KeyDown="FileFilterTextBox_KeyDown"
                                 HorizontalAlignment="Stretch"/>
                    </Grid>

                    <!-- 排除目录 -->
                    <Grid Width="140" Margin="0,0,4,0">
                        <Button x:Name="ExcludeButton"
                                Style="{StaticResource TagButton}"
                                Content="{Binding ExcludeDirsDisplay}"
                                Click="ExcludeButton_Click"
                                HorizontalAlignment="Stretch"/>
                        <TextBox x:Name="ExcludeTextBox"
                                 Style="{StaticResource TagInput}"
                                 Text="{Binding ExcludeDirs, UpdateSourceTrigger=PropertyChanged}"
                                 Visibility="Collapsed"
                                 LostFocus="ExcludeTextBox_LostFocus"
                                 KeyDown="ExcludeTextBox_KeyDown"
                                 HorizontalAlignment="Stretch"/>
                    </Grid>

                    <!-- 分隔线 -->
                    <TextBlock Text="|" Foreground="#e0e0e0" Margin="4,0,4,0"
                               VerticalAlignment="Center" FontSize="12"/>

                    <!-- 日期范围 -->
                    <CheckBox x:Name="DateToggle"
                              Style="{StaticResource ToggleOptionButton}"
                              IsChecked="{Binding DateFilterEnabled}"
                              Content="&#x1F4C5; 日期范围"/>

                    <!-- 日期选择（内联，仅启用时显示） -->
                    <DatePicker SelectedDate="{Binding DateFrom}" Width="120"
                                Margin="0,0,4,0" VerticalAlignment="Center">
                        <DatePicker.Style>
                            <Style TargetType="DatePicker">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding DateFilterEnabled}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DatePicker.Style>
                    </DatePicker>
                    <TextBlock Text=" ~ " VerticalAlignment="Center" Foreground="#9a9a9a">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding DateFilterEnabled}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <DatePicker SelectedDate="{Binding DateTo}" Width="120"
                                Margin="4,0,0,0" VerticalAlignment="Center">
                        <DatePicker.Style>
                            <Style TargetType="DatePicker">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding DateFilterEnabled}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DatePicker.Style>
                    </DatePicker>

                    <!-- 弹性空间 -->
                    <Border Width="10"/>

                    <!-- 操作按钮 -->
                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="搜索" Command="{Binding SearchCommand}"
                                Style="{StaticResource SearchButton}" IsDefault="True"/>
                        <Button Content="取消" Command="{Binding CancelCommand}"
                                Style="{StaticResource CancelButton}"/>
                    </WrapPanel>
                </WrapPanel>
            </StackPanel>
        </Border>

        <!-- ==================== Tab Bar ==================== -->
        <Border DockPanel.Dock="Top" Background="#fff"
                BorderBrush="#e0e0e0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Margin="16,0,0,0">
                <RadioButton x:Name="TabResults" GroupName="MainTabs"
                             Style="{StaticResource TabButtonStyle}"
                             IsChecked="True"
                             Checked="TabResults_Checked">
                    <TextBlock Text="搜索结果"/>
                </RadioButton>
                <RadioButton x:Name="TabHistory" GroupName="MainTabs"
                             Style="{StaticResource TabButtonStyle}"
                             Checked="TabHistory_Checked">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="搜索历史" VerticalAlignment="Center"/>
                        <Border CornerRadius="7" Padding="5,1" Margin="4,0,0,0">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="#9a9a9a"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsChecked, ElementName=TabHistory}" Value="True">
                                            <Setter Property="Background" Value="#0e639c"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="{Binding HistoryEntries.Count, FallbackValue=0}"
                                       FontSize="10" Foreground="White" VerticalAlignment="Center"/>
                        </Border>
                    </StackPanel>
                </RadioButton>
            </StackPanel>
        </Border>

        <!-- ==================== Status Bar ==================== -->
        <Border DockPanel.Dock="Bottom" Background="#e8e8e8"
                BorderBrush="#e0e0e0" BorderThickness="0,1,0,0" Padding="12,5">
            <Grid>
                <TextBlock Foreground="#555" FontSize="12" VerticalAlignment="Center">
                    <Run Text="{Binding StatusText, Mode=OneWay}"/>
                </TextBlock>
                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                    <TextBlock Foreground="#555" FontSize="12" VerticalAlignment="Center">
                        <Run Text="文件: "/><Run Text="{Binding TotalFiles, Mode=OneWay}" Foreground="#0e639c" FontWeight="Bold"/>
                    </TextBlock>
                    <TextBlock Foreground="#555" FontSize="12" Margin="16,0,0,0" VerticalAlignment="Center">
                        <Run Text="匹配: "/><Run Text="{Binding TotalMatches, Mode=OneWay}" Foreground="#0e639c" FontWeight="Bold"/>
                    </TextBlock>
                    <TextBlock Foreground="#555" FontSize="12" Margin="16,0,0,0" VerticalAlignment="Center">
                        <Run Text="耗时: "/><Run Text="{Binding ElapsedTime, Mode=OneWay}" Foreground="#0e639c" FontWeight="Bold"/>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ==================== Content Area ==================== -->
        <Grid>
            <!-- ===== 搜索结果视图 ===== -->
            <Grid x:Name="ResultsView">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsHistoryTab}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                <Grid.RowDefinitions>
                    <RowDefinition Height="2*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="3*"/>
                </Grid.RowDefinitions>

                <!-- 文件列表 -->
                <DataGrid Grid.Row="0"
                          ItemsSource="{Binding SearchResults}"
                          SelectedItem="{Binding SelectedResult}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#f0f0f0"
                          BorderThickness="0"
                          Background="White"
                          RowBackground="White"
                          AlternatingRowBackground="White"
                          MouseDoubleClick="DataGrid_MouseDoubleClick">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#fafafa"/>
                            <Setter Property="Foreground" Value="#9a9a9a"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="Padding" Value="8,7"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="BorderBrush" Value="#e0e0e0"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#e8e8e8"/>
                                    <Setter Property="Foreground" Value="#1e1e1e"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="FontSize" Value="13"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#e8e8e8"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#cce5ff"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="6,4"/>
                            <Setter Property="Focusable" Value="False"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Foreground" Value="#1e1e1e"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="打开文件" Command="{Binding OpenFileCommand}"
                                      InputGestureText="双击"/>
                            <MenuItem Header="在资源管理器中打开" Command="{Binding OpenInExplorerCommand}"/>
                            <Separator/>
                            <MenuItem Header="复制文件名" Command="{Binding CopyFileNameCommand}"/>
                            <MenuItem Header="复制文件路径" Command="{Binding CopyPathCommand}"/>
                        </ContextMenu>
                    </DataGrid.ContextMenu>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="文件名" Binding="{Binding FileName}" Width="200">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Medium"/>
                                    <Setter Property="Foreground" Value="#1e1e1e"/>
                                    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Padding" Value="10,0,0,0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="大小" Width="90"
                                            Binding="{Binding FileSizeDisplay}"
                                            SortMemberPath="FileSize">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#6a6a6a"/>
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTemplateColumn Header="匹配数" Width="70" SortMemberPath="MatchCount">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="#0e639c" CornerRadius="9" Padding="6,1"
                                            HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding MatchCount}" Foreground="White"
                                                   FontWeight="Bold" FontSize="12"
                                                   HorizontalAlignment="Center"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="路径" Binding="{Binding FilePath}" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#9a9a9a"/>
                                    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="修改时间" Binding="{Binding LastModified, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="130">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#9a9a9a"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Padding" Value="0,0,10,0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- 分割线 -->
                <GridSplitter Grid.Row="1" Height="3" HorizontalAlignment="Stretch"
                              Background="#e0e0e0"
                              DragCompleted="Splitter_DragCompleted"/>

                <!-- 下半区：匹配行 + 预览 -->
                <Grid x:Name="BottomPanelGrid" Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="35*" MinWidth="200"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="65*" MinWidth="300"/>
                    </Grid.ColumnDefinitions>

                    <!-- 左侧：匹配行 -->
                    <Grid Grid.Column="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Style="{StaticResource PanelHeader}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="匹配行" FontWeight="SemiBold" FontSize="12" Foreground="#9a9a9a"/>
                                <TextBlock Text="{Binding SelectedResult.MatchCount, FallbackValue=0}"
                                           FontWeight="Bold" FontSize="12" Foreground="#c0392b" Margin="4,0"/>
                                <TextBlock Text="—" FontSize="12" Foreground="#9a9a9a" Margin="4,0"/>
                                <TextBlock Text="{Binding SelectedResult.FileName, FallbackValue=''}"
                                           FontSize="12" Foreground="#9a9a9a"/>
                            </StackPanel>
                        </Border>
                        <ListBox Grid.Row="1"
                                 ItemsSource="{Binding MatchLines}"
                                 SelectedItem="{Binding SelectedMatchLine}"
                                 MouseDoubleClick="MatchList_MouseDoubleClick"
                                 BorderThickness="0"
                                 FontFamily="Consolas" FontSize="12"
                                 Background="White">
                            <ListBox.Resources>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Height" Value="30"/>
                                    <Setter Property="Padding" Value="4,0"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                                    <Setter Property="Cursor" Value="Hand"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="#fffbe6"/>
                                            <Setter Property="BorderBrush" Value="#b8860b"/>
                                            <Setter Property="BorderThickness" Value="3,0,0,0"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#e8e8e8"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ListBox.Resources>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock FontFamily="Consolas" FontSize="12">
                                        <Run Text="{Binding LineNumber, StringFormat='{}{0,5}'}"
                                             Foreground="#9a9a9a"/>
                                        <Run Text="  "/>
                                        <Run Text="{Binding DisplayText}" Foreground="#6a6a6a"/>
                                    </TextBlock>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>

                    <GridSplitter Grid.Column="1" Width="2" HorizontalAlignment="Stretch"
                                  Background="#e0e0e0" VerticalAlignment="Stretch"
                                  DragCompleted="Splitter_DragCompleted"/>

                    <!-- 右侧：文件预览 -->
                    <Grid Grid.Column="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Style="{StaticResource PanelHeader}">
                            <DockPanel>
                                <CheckBox DockPanel.Dock="Right"
                                          Content="自动换行"
                                          IsChecked="{Binding PreviewWordWrap}"
                                          VerticalAlignment="Center"
                                          FontSize="12" Foreground="#9a9a9a"
                                          Cursor="Hand"/>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="文件预览" FontWeight="SemiBold" FontSize="12" Foreground="#9a9a9a"/>
                                    <TextBlock Text=" — " FontSize="12" Foreground="#9a9a9a"/>
                                    <TextBlock Text="{Binding SelectedResult.FileName, FallbackValue=''}"
                                               FontSize="12" Foreground="#9a9a9a"/>
                                </StackPanel>
                            </DockPanel>
                        </Border>
                        <Grid Grid.Row="1">
                            <avalonedit:TextEditor Name="PreviewEditor"
                                IsReadOnly="True"
                                ShowLineNumbers="True"
                                WordWrap="{Binding PreviewWordWrap}"
                                FontFamily="Consolas"
                                FontSize="13"
                                Background="White"/>

                            <TextBlock Text="二进制文件，无法预览"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                       Foreground="Gray" FontSize="15">
                                <TextBlock.Visibility>
                                    <Binding Path="IsBinaryFile">
                                        <Binding.Converter>
                                            <BooleanToVisibilityConverter/>
                                        </Binding.Converter>
                                    </Binding>
                                </TextBlock.Visibility>
                            </TextBlock>

                            <TextBlock Text="{Binding FileErrorMessage}"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                       Foreground="Gray" FontSize="15">
                                <TextBlock.Visibility>
                                    <Binding Path="IsFileError">
                                        <Binding.Converter>
                                            <BooleanToVisibilityConverter/>
                                        </Binding.Converter>
                                    </Binding>
                                </TextBlock.Visibility>
                            </TextBlock>

                            <TextBlock Text="文件过大，仅显示部分内容"
                                       HorizontalAlignment="Right" VerticalAlignment="Top"
                                       Foreground="#b8860b" FontSize="12" Margin="4"
                                       Panel.ZIndex="1">
                                <TextBlock.Visibility>
                                    <Binding Path="IsFileTruncated">
                                        <Binding.Converter>
                                            <BooleanToVisibilityConverter/>
                                        </Binding.Converter>
                                    </Binding>
                                </TextBlock.Visibility>
                            </TextBlock>
                        </Grid>
                    </Grid>
                </Grid>
            </Grid>

            <!-- ===== 搜索历史视图 ===== -->
            <Grid x:Name="HistoryView">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsHistoryTab}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- 工具栏 -->
                <Border Grid.Row="0" Background="#fafafa" Padding="16,8"
                        BorderBrush="#e0e0e0" BorderThickness="0,0,0,1">
                    <DockPanel>
                        <TextBlock Text="搜索历史" FontWeight="SemiBold" FontSize="14" Foreground="#1e1e1e"
                                   VerticalAlignment="Center"/>
                        <TextBlock Text="点击任意记录可恢复搜索条件" FontSize="12" Foreground="#9a9a9a"
                                   Margin="10,0,0,0" VerticalAlignment="Center"/>
                        <Button DockPanel.Dock="Right" Content="清空历史"
                                Command="{Binding ClearHistoryCommand}"
                                Foreground="#c0392b" Background="Transparent"
                                BorderThickness="0" FontSize="12" Cursor="Hand" Padding="8,4"/>
                    </DockPanel>
                </Border>

                <!-- 历史卡片列表 -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding HistoryEntries}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type models:SearchHistoryEntry}">
                                <Border BorderBrush="#f0f0f0" BorderThickness="0,0,0,1"
                                        Padding="16,10" Background="Transparent"
                                        MouseLeftButtonUp="HistoryCard_Click"
                                        Cursor="Hand">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#e8e8e8"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Border.InputBindings>
                                        <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding DataContext.SearchWithHistoryCommand, ElementName=HistoryView}" CommandParameter="{Binding}"/>
                                    </Border.InputBindings>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="90"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- 左: 搜索词+路径+标签 -->
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding SearchText}"
                                                       FontFamily="Consolas" FontWeight="Bold"
                                                       FontSize="14" Foreground="#1e1e1e"/>
                                            <TextBlock Text="{Binding SearchPath}"
                                                       FontSize="12" Foreground="#9a9a9a" Margin="0,2,0,4"/>
                                            <WrapPanel>
                                                <!-- 正则/文本标签 -->
                                                <Border CornerRadius="3" Padding="4,2" Margin="0,0,4,0">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Background" Value="#f0f0f0"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsRegex}" Value="True">
                                                                    <Setter Property="Background" Value="#cce5ff"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <TextBlock FontSize="10">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Foreground" Value="#9a9a9a"/>
                                                                <Setter Property="Text" Value="文本"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsRegex}" Value="True">
                                                                        <Setter Property="Foreground" Value="#0e639c"/>
                                                                        <Setter Property="Text" Value="正则"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </Border>
                                                <!-- 区分大小写 -->
                                                <Border Background="#cce5ff" CornerRadius="3" Padding="4,2" Margin="0,0,4,0">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding CaseSensitive}" Value="True">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <TextBlock Text="区分大小写" FontSize="10" Foreground="#0e639c"/>
                                                </Border>
                                                <!-- 全词 -->
                                                <Border Background="#cce5ff" CornerRadius="3" Padding="4,2" Margin="0,0,4,0">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding WholeWord}" Value="True">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <TextBlock Text="全词" FontSize="10" Foreground="#0e639c"/>
                                                </Border>
                                                <!-- 文件过滤 -->
                                                <Border Background="#f0f0f0" CornerRadius="3" Padding="4,2" Margin="0,0,4,0">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding FileFilter}" Value="">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <TextBlock Text="{Binding FileFilter}" FontSize="10" Foreground="#9a9a9a"/>
                                                </Border>
                                                <!-- 排除目录 -->
                                                <Border Background="#f0f0f0" CornerRadius="3" Padding="4,2" Margin="0,0,4,0">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding ExcludeDirs}" Value="">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <TextBlock Text="{Binding ExcludeDirs, StringFormat='排除: {0}'}" FontSize="10" Foreground="#9a9a9a"/>
                                                </Border>
                                            </WrapPanel>
                                        </StackPanel>

                                        <!-- 中: 统计 -->
                                        <StackPanel Grid.Column="1" Orientation="Horizontal"
                                                    VerticalAlignment="Center" Margin="16,0">
                                            <TextBlock FontSize="12" Foreground="#9a9a9a">
                                                <Run Text="{Binding SearchedFiles, StringFormat='{}{0:N0}', Mode=OneWay}"
                                                     Foreground="#0e639c" FontWeight="Bold"/>
                                                <Run Text=" 文件"/>
                                            </TextBlock>
                                            <TextBlock FontSize="12" Foreground="#9a9a9a" Margin="10,0,0,0">
                                                <Run Text="{Binding FoundFiles, Mode=OneWay}"
                                                     Foreground="#0e639c" FontWeight="Bold"/>
                                                <Run Text=" 命中"/>
                                            </TextBlock>
                                            <TextBlock FontSize="12" Foreground="#9a9a9a" Margin="10,0,0,0">
                                                <Run Text="{Binding TotalMatches, Mode=OneWay}"
                                                     Foreground="#0e639c" FontWeight="Bold"/>
                                                <Run Text=" 匹配"/>
                                            </TextBlock>
                                        </StackPanel>

                                        <!-- 右: 时间 -->
                                        <TextBlock Grid.Column="2"
                                                   Text="{Binding SearchedAt, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}"
                                                   FontSize="11" Foreground="#bcbcbc"
                                                   VerticalAlignment="Center"
                                                   HorizontalAlignment="Right"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Grid>

    </DockPanel>
</Window>
