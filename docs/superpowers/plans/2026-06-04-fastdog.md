# FastDog 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建一个基于 WPF + ripgrep 的文本搜索工具，供团队内部使用。

**Architecture:** 三层架构：WPF UI (MVVM) → SearchService (业务逻辑，参数构建，日期过滤) → RipgrepBridge (rg.exe 进程管理，JSON 输出解析)。ripgrep 负责所有文本搜索。

**Tech Stack:** .NET 8, WPF, CommunityToolkit.Mvvm, System.Text.Json, xUnit

---

## 文件结构

```
E:\demo\ai\my\fastdog\FastDog\
├── FastDog.sln
├── src\
│   └── FastDog\
│       ├── FastDog.csproj
│       ├── App.xaml / App.xaml.cs
│       ├── MainWindow.xaml / MainWindow.xaml.cs
│       ├── ViewModels\
│       │   └── MainViewModel.cs
│       ├── Services\
│       │   ├── SearchService.cs
│       │   └── RipgrepBridge.cs
│       └── Models\
│           ├── SearchQuery.cs
│           ├── SearchResult.cs
│           └── MatchLine.cs
├── tests\
│   └── FastDog.Tests\
│       ├── FastDog.Tests.csproj
│       ├── ArgumentBuilderTests.cs
│       ├── JsonParserTests.cs
│       └── DateFilterTests.cs
└── tools\
    └── rg.exe  (手动放入)
```

---

### Task 1: 项目脚手架

**Files:**
- Create: `FastDog.sln`
- Create: `src/FastDog/FastDog.csproj`
- Create: `tests/FastDog.Tests/FastDog.Tests.csproj`

- [ ] **Step 1: 创建解决方案和 WPF 项目**

```powershell
cd E:\demo\ai\my\fastdog
dotnet new sln -n FastDog
dotnet new wpf -n FastDog -o src\FastDog -f net8.0-windows
dotnet sln FastDog.sln add src\FastDog\FastDog.csproj
```

- [ ] **Step 2: 创建测试项目并添加引用**

```powershell
dotnet new xunit -n FastDog.Tests -o tests\FastDog.Tests -f net8.0-windows
dotnet sln FastDog.sln add tests\FastDog.Tests\FastDog.Tests.csproj
cd tests\FastDog.Tests
dotnet add reference ..\..\src\FastDog\FastDog.csproj
cd ..\..
```

- [ ] **Step 3: 添加 CommunityToolkit.Mvvm 包**

```powershell
cd src\FastDog
dotnet add package CommunityToolkit.Mvvm
cd ..\..
```

- [ ] **Step 4: 创建目录结构**

```powershell
mkdir src\FastDog\ViewModels
mkdir src\FastDog\Services
mkdir src\FastDog\Models
mkdir tools
```

- [ ] **Step 5: 放入 rg.exe**

将 rg.exe 复制到 `tools\rg.exe`。可从 https://github.com/BurntSushi/ripgrep/releases 下载 Windows 版本。

- [ ] **Step 6: 验证构建**

```powershell
dotnet build FastDog.sln
```

Expected: BUILD SUCCEEDED

- [ ] **Step 7: 提交**

```powershell
git init
git add .
git commit -m "chore: scaffold FastDog solution with WPF project and test project"
```

---

### Task 2: 数据模型

**Files:**
- Create: `src/FastDog/Models/SearchQuery.cs`
- Create: `src/FastDog/Models/SearchResult.cs`
- Create: `src/FastDog/Models/MatchLine.cs`

- [ ] **Step 1: 创建 SearchQuery.cs**

用户搜索输入的封装。

```csharp
// src/FastDog/Models/SearchQuery.cs
namespace FastDog.Models;

public class SearchQuery
{
    public string SearchPath { get; set; } = string.Empty;
    public string SearchText { get; set; } = string.Empty;
    public bool IsRegex { get; set; } = true;
    public bool CaseSensitive { get; set; } = false;
    public bool WholeWord { get; set; } = false;
    public string FileFilter { get; set; } = string.Empty;
    public string ExcludeDirs { get; set; } = string.Empty;
    public bool DateFilterEnabled { get; set; } = false;
    public DateTime? DateFrom { get; set; }
    public DateTime? DateTo { get; set; }
}
```

- [ ] **Step 2: 创建 MatchLine.cs**

单个匹配行的信息。

```csharp
// src/FastDog/Models/MatchLine.cs
namespace FastDog.Models;

public class MatchLine
{
    public int LineNumber { get; set; }
    public string LineText { get; set; } = string.Empty;
    public int MatchStart { get; set; }
    public int MatchEnd { get; set; }
}
```

- [ ] **Step 3: 创建 SearchResult.cs**

文件级别的搜索结果，包含该文件所有匹配行。

```csharp
// src/FastDog/Models/SearchResult.cs
namespace FastDog.Models;

public class SearchResult
{
    public string FilePath { get; set; } = string.Empty;
    public string FileName => Path.GetFileName(FilePath);
    public string RelativePath { get; set; } = string.Empty;
    public long FileSize { get; set; }
    public int MatchCount { get; set; }
    public DateTime LastModified { get; set; }
    public List<MatchLine> Matches { get; set; } = [];
}
```

- [ ] **Step 4: 验证构建**

```powershell
dotnet build FastDog.sln
```

Expected: BUILD SUCCEEDED

- [ ] **Step 5: 提交**

```powershell
git add src\FastDog\Models\
git commit -m "feat: add data models (SearchQuery, SearchResult, MatchLine)"
```

---

### Task 3: RipgrepBridge - 参数构建 (TDD)

**Files:**
- Create: `tests/FastDog.Tests/ArgumentBuilderTests.cs`
- Create: `src/FastDog/Services/RipgrepBridge.cs` (部分)

- [ ] **Step 1: 编写参数构建测试**

```csharp
// tests/FastDog.Tests/ArgumentBuilderTests.cs
using FastDog.Services;
using FastDog.Models;

namespace FastDog.Tests;

public class ArgumentBuilderTests
{
    [Fact]
    public void BuildArguments_BasicRegexSearch()
    {
        var query = new SearchQuery
        {
            SearchPath = @"E:\code",
            SearchText = "class",
            IsRegex = true
        };
        var args = RipgrepBridge.BuildArguments(query);
        Assert.Contains("--json", args);
        Assert.Contains("-i", args); // 默认不区分大小写
        Assert.Contains(@"""E:\code""", args);
        Assert.Contains(@"""class""", args);
        Assert.DoesNotContain("-F", args);
    }

    [Fact]
    public void BuildArguments_PlainTextSearch()
    {
        var query = new SearchQuery
        {
            SearchPath = @"E:\code",
            SearchText = "hello world",
            IsRegex = false
        };
        var args = RipgrepBridge.BuildArguments(query);
        Assert.Contains("-F", args);
    }

    [Fact]
    public void BuildArguments_CaseSensitive()
    {
        var query = new SearchQuery
        {
            SearchPath = @"E:\code",
            SearchText = "test",
            CaseSensitive = true
        };
        var args = RipgrepBridge.BuildArguments(query);
        Assert.DoesNotContain("-i", args);
    }

    [Fact]
    public void BuildArguments_WholeWord()
    {
        var query = new SearchQuery
        {
            SearchPath = @"E:\code",
            SearchText = "test",
            WholeWord = true
        };
        var args = RipgrepBridge.BuildArguments(query);
        Assert.Contains("-w", args);
    }

    [Fact]
    public void BuildArguments_FileFilter()
    {
        var query = new SearchQuery
        {
            SearchPath = @"E:\code",
            SearchText = "test",
            FileFilter = "*.cs;*.txt"
        };
        var args = RipgrepBridge.BuildArguments(query);
        Assert.Contains("--iglob", args);
        Assert.Contains("*.cs", args);
        Assert.Contains("*.txt", args);
    }

    [Fact]
    public void BuildArguments_ExcludeDirs()
    {
        var query = new SearchQuery
        {
            SearchPath = @"E:\code",
            SearchText = "test",
            ExcludeDirs = "bin;obj;.git"
        };
        var args = RipgrepBridge.BuildArguments(query);
        Assert.Contains("--glob", args);
        Assert.Contains("!bin", args);
        Assert.Contains("!obj", args);
        Assert.Contains("!.git", args);
    }

    [Fact]
    public void BuildArguments_DefaultExcludesGit()
    {
        var query = new SearchQuery
        {
            SearchPath = @"E:\code",
            SearchText = "test",
            ExcludeDirs = "bin;obj"
        };
        var args = RipgrepBridge.BuildArguments(query);
        Assert.Contains("!.git", args); // 默认始终排除 .git
    }

    [Fact]
    public void BuildArguments_AllOptionsCombined()
    {
        var query = new SearchQuery
        {
            SearchPath = @"E:\code\project",
            SearchText = @"\bclass\b",
            IsRegex = true,
            CaseSensitive = true,
            WholeWord = false,
            FileFilter = "*.cs",
            ExcludeDirs = "bin;obj"
        };
        var args = RipgrepBridge.BuildArguments(query);
        Assert.Contains("--json", args);
        Assert.DoesNotContain("-i", args);
        Assert.DoesNotContain("-F", args);
        Assert.Contains("--iglob", args);
        Assert.Contains("*.cs", args);
        Assert.Contains("!.git", args);
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

```powershell
cd tests\FastDog.Tests
dotnet test --filter "FullyQualifiedName~ArgumentBuilderTests" -v n
```

Expected: 编译失败，RipgrepBridge 类不存在

- [ ] **Step 3: 实现 RipgrepBridge.BuildArguments**

```csharp
// src/FastDog/Services/RipgrepBridge.cs
using System.Text;
using System.Text.Json;
using FastDog.Models;

namespace FastDog.Services;

public sealed class RipgrepBridge : IDisposable
{
    private Process? _process;

    public static string FindRgPath()
    {
        // 优先查找应用目录下的 tools/rg.exe
        var appDir = AppDomain.CurrentDomain.BaseDirectory;
        var localRg = Path.Combine(appDir, "tools", "rg.exe");
        if (File.Exists(localRg))
            return localRg;

        // 回退到系统 PATH
        var pathRg = FindInPath("rg.exe");
        if (pathRg is not null)
            return pathRg;

        throw new FileNotFoundException(
            "找不到 rg.exe。请将其放入 tools/ 目录或添加到系统 PATH。");
    }

    private static string? FindInPath(string fileName)
    {
        var pathEnv = Environment.GetEnvironmentVariable("PATH") ?? "";
        var separator = Path.PathSeparator;
        foreach (var dir in pathEnv.Split(separator, StringSplitOptions.RemoveEmptyEntries))
        {
            var fullPath = Path.Combine(dir, fileName);
            if (File.Exists(fullPath))
                return fullPath;
        }
        return null;
    }

    public static string BuildArguments(SearchQuery query)
    {
        var sb = new StringBuilder();
        sb.Append("--json --no-heading ");

        // 大小写：默认不敏感（加 -i），勾选后不加
        if (!query.CaseSensitive)
            sb.Append("-i ");

        // 纯文本模式
        if (!query.IsRegex)
            sb.Append("-F ");

        // 全词匹配
        if (query.WholeWord)
            sb.Append("-w ");

        // 文件过滤（分号分隔 → 多个 --iglob）
        if (!string.IsNullOrWhiteSpace(query.FileFilter))
        {
            foreach (var pattern in query.FileFilter.Split(';', StringSplitOptions.RemoveEmptyEntries))
            {
                var trimmed = pattern.Trim();
                if (trimmed.Length > 0)
                    sb.Append($"--iglob \"{trimmed}\" ");
            }
        }

        // 排除目录
        var excludeDirs = new List<string>();
        if (!string.IsNullOrWhiteSpace(query.ExcludeDirs))
        {
            foreach (var dir in query.ExcludeDirs.Split(';', StringSplitOptions.RemoveEmptyEntries))
            {
                var trimmed = dir.Trim();
                if (trimmed.Length > 0)
                    excludeDirs.Add(trimmed);
            }
        }
        // 默认始终排除 .git
        if (!excludeDirs.Contains(".git"))
            excludeDirs.Add(".git");

        foreach (var dir in excludeDirs)
            sb.Append($"--glob \"!{dir}\" ");

        // 搜索路径
        sb.Append($"\"{query.SearchPath}\" ");

        // 搜索文本（放最后）
        sb.Append($"\"{query.SearchText}\"");

        return sb.ToString();
    }

    // Task 4 中补充 SearchAsync 和 Dispose
    public void Dispose()
    {
        _process?.Kill();
        _process?.Dispose();
    }
}
```

- [ ] **Step 4: 运行测试确认通过**

```powershell
cd tests\FastDog.Tests
dotnet test --filter "FullyQualifiedName~ArgumentBuilderTests" -v n
```

Expected: 全部 PASSED

- [ ] **Step 5: 提交**

```powershell
git add src\FastDog\Services\RipgrepBridge.cs tests\FastDog.Tests\ArgumentBuilderTests.cs
git commit -m "feat: implement rg argument builder with tests"
```

---

### Task 4: RipgrepBridge - JSON 解析 (TDD)

**Files:**
- Create: `tests/FastDog.Tests/JsonParserTests.cs`
- Modify: `src/FastDog/Services/RipgrepBridge.cs` (添加 JSON 解析和 SearchAsync)

- [ ] **Step 1: 编写 JSON 解析测试**

```csharp
// tests/FastDog.Tests/JsonParserTests.cs
using FastDog.Services;

namespace FastDog.Tests;

public class JsonParserTests
{
    [Fact]
    public void ParseBeginMessage()
    {
        var json = @"{""type"":""begin"",""data"":{""path"":{""text"":""E:\\code\\App.cs""}}}";
        var result = RipgrepBridge.ParseRgLine(json);
        Assert.NotNull(result);
        Assert.Equal(RgEventType.FileBegin, result.Type);
        Assert.Equal(@"E:\code\App.cs", result.FilePath);
    }

    [Fact]
    public void ParseMatchMessage()
    {
        var json = @"{""type"":""match"",""data"":{""path"":{""text"":""E:\\code\\App.cs""},""lines"":{""text"":""public class App\r\n""},""line_number"":5,""absolute_offset"":120,""submatches"":[{""match"":{""text"":""class""},""start"":7,""end"":12}]}}";
        var result = RipgrepBridge.ParseRgLine(json);
        Assert.NotNull(result);
        Assert.Equal(RgEventType.Match, result.Type);
        Assert.Equal(@"E:\code\App.cs", result.FilePath);
        Assert.Equal(5, result.LineNumber);
        Assert.Equal("public class App\r\n", result.LineText);
        Assert.Equal(7, result.MatchStart);
        Assert.Equal(12, result.MatchEnd);
    }

    [Fact]
    public void ParseEndMessage()
    {
        var json = @"{""type"":""end"",""data"":{""path"":{""text"":""E:\\code\\App.cs""},""binary_offset"":null,""stats"":{""elapsed"":{""secs"":0,""nanos"":23400,""human"":""0.000023s""},""searches"":1,""searches_with_match"":1,""bytes_searched"":33782,""bytes_printed"":394,""matched_lines"":2,""matches"":3}}}";
        var result = RipgrepBridge.ParseRgLine(json);
        Assert.NotNull(result);
        Assert.Equal(RgEventType.FileEnd, result.Type);
        Assert.Equal(@"E:\code\App.cs", result.FilePath);
    }

    [Fact]
    public void ParseSummaryMessage()
    {
        var json = @"{""data"":{""elapsed_total"":{""human"":""0.003853s"",""nanos"":3853100,""secs"":0},""stats"":{""bytes_printed"":394,""bytes_searched"":33782,""elapsed"":{""human"":""0.000023s"",""nanos"":23400,""secs"":0},""matched_lines"":2,""matches"":3,""searches"":1,""searches_with_match"":1}},""type"":""summary""}";
        var result = RipgrepBridge.ParseRgLine(json);
        Assert.NotNull(result);
        Assert.Equal(RgEventType.Summary, result.Type);
        Assert.Equal(3L, result.TotalMatches);
        Assert.Equal(2L, result.MatchedLines);
    }

    [Fact]
    public void ParseNull_ReturnsNull()
    {
        var result = RipgrepBridge.ParseRgLine("");
        Assert.Null(result);
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

```powershell
cd tests\FastDog.Tests
dotnet test --filter "FullyQualifiedName~JsonParserTests" -v n
```

Expected: 编译失败，RgEventType、ParseRgLine 不存在

- [ ] **Step 3: 添加 RgEvent 模型和 ParseRgLine 方法到 RipgrepBridge**

在 `RipgrepBridge.cs` 中添加以下内容：

```csharp
// 在 RipgrepBridge.cs 文件顶部，namespace 内部添加

public enum RgEventType
{
    FileBegin,
    Match,
    FileEnd,
    Summary
}

public class RgEvent
{
    public RgEventType Type { get; set; }
    public string FilePath { get; set; } = string.Empty;
    public int LineNumber { get; set; }
    public string LineText { get; set; } = string.Empty;
    public int MatchStart { get; set; }
    public int MatchEnd { get; set; }
    public long TotalMatches { get; set; }
    public long MatchedLines { get; set; }
    public string Elapsed { get; set; } = string.Empty;
}
```

然后在 `RipgrepBridge` 类中添加 `ParseRgLine` 方法：

```csharp
// 添加到 RipgrepBridge 类内部

public static RgEvent? ParseRgLine(string jsonLine)
{
    if (string.IsNullOrWhiteSpace(jsonLine))
        return null;

    using var doc = JsonDocument.Parse(jsonLine);
    var root = doc.RootElement;

    var type = root.GetProperty("type").GetString() ?? "";
    var data = root.GetProperty("data");

    return type switch
    {
        "begin" => new RgEvent
        {
            Type = RgEventType.FileBegin,
            FilePath = data.GetProperty("path").GetProperty("text").GetString() ?? ""
        },
        "match" => ParseMatch(data),
        "end" => new RgEvent
        {
            Type = RgEventType.FileEnd,
            FilePath = data.GetProperty("path").GetProperty("text").GetString() ?? ""
        },
        "summary" => ParseSummary(data),
        _ => null
    };
}

private static RgEvent ParseMatch(JsonElement data)
{
    var submatches = data.GetProperty("submatches");
    int matchStart = 0, matchEnd = 0;
    if (submatches.GetArrayLength() > 0)
    {
        var first = submatches[0].GetProperty("match");
        matchStart = first.GetProperty("start").GetInt32();
        matchEnd = first.GetProperty("end").GetInt32();
    }

    return new RgEvent
    {
        Type = RgEventType.Match,
        FilePath = data.GetProperty("path").GetProperty("text").GetString() ?? "",
        LineNumber = data.GetProperty("line_number").GetInt32(),
        LineText = data.GetProperty("lines").GetProperty("text").GetString() ?? "",
        MatchStart = matchStart,
        MatchEnd = matchEnd
    };
}

private static RgEvent ParseSummary(JsonElement data)
{
    var stats = data.GetProperty("stats");
    var elapsed = data.GetProperty("elapsed_total");
    return new RgEvent
    {
        Type = RgEventType.Summary,
        TotalMatches = stats.GetProperty("matches").GetInt64(),
        MatchedLines = stats.GetProperty("matched_lines").GetInt64(),
        Elapsed = elapsed.GetProperty("human").GetString() ?? ""
    };
}
```

确保文件顶部有 `using System.Text.Json;`。

- [ ] **Step 4: 运行测试确认通过**

```powershell
cd tests\FastDog.Tests
dotnet test --filter "FullyQualifiedName~JsonParserTests" -v n
```

Expected: 全部 PASSED

- [ ] **Step 5: 添加 SearchAsync 方法到 RipgrepBridge**

```csharp
// 添加到 RipgrepBridge 类内部

public async IAsyncEnumerable<RgEvent> SearchAsync(
    string arguments,
    [EnumeratorCancellation] CancellationToken ct = default)
{
    var rgPath = FindRgPath();
    var psi = new ProcessStartInfo
    {
        FileName = rgPath,
        Arguments = arguments,
        UseShellExecute = false,
        CreateNoWindow = true,
        RedirectStandardOutput = true,
        RedirectStandardError = true,
        StandardOutputEncoding = Encoding.UTF8
    };

    _process = Process.Start(psi) ?? throw new InvalidOperationException("无法启动 rg.exe");

    var reader = _process.StandardOutput;
    while (!reader.EndOfStream && !ct.IsCancellationRequested)
    {
        var line = await reader.ReadLineAsync(ct);
        if (line is null) break;
        var rgEvent = ParseRgLine(line);
        if (rgEvent is not null)
            yield return rgEvent;
    }

    if (!ct.IsCancellationRequested)
        await _process.WaitForExitAsync(ct);
    else
        KillProcess();
}

public void KillProcess()
{
    try
    {
        _process?.Kill(entireProcessTree: true);
    }
    catch { /* 进程可能已退出 */ }
}
```

确保文件顶部有 `using System.Runtime.CompilerServices;`。

- [ ] **Step 6: 验证完整构建**

```powershell
dotnet build FastDog.sln
```

Expected: BUILD SUCCEEDED

- [ ] **Step 7: 运行所有测试**

```powershell
dotnet test FastDog.sln -v n
```

Expected: 全部 PASSED

- [ ] **Step 8: 提交**

```powershell
git add src\FastDog\Services\RipgrepBridge.cs tests\FastDog.Tests\JsonParserTests.cs
git commit -m "feat: add rg JSON parser and SearchAsync to RipgrepBridge"
```

---

### Task 5: SearchService - 业务逻辑 (TDD)

**Files:**
- Create: `tests/FastDog.Tests/DateFilterTests.cs`
- Create: `src/FastDog/Services/SearchService.cs`

- [ ] **Step 1: 编写日期过滤测试**

```csharp
// tests/FastDog.Tests/DateFilterTests.cs
using FastDog.Services;
using FastDog.Models;

namespace FastDog.Tests;

public class DateFilterTests
{
    [Fact]
    public void FilterByDate_WithinRange()
    {
        var results = new List<SearchResult>
        {
            new() { FilePath = "a.cs", LastModified = new DateTime(2026, 5, 15) }
        };
        var filtered = SearchService.FilterByDate(
            results, new DateTime(2026, 5, 1), new DateTime(2026, 6, 1));
        Assert.Single(filtered);
    }

    [Fact]
    public void FilterByDate_BeforeRange()
    {
        var results = new List<SearchResult>
        {
            new() { FilePath = "a.cs", LastModified = new DateTime(2026, 4, 15) }
        };
        var filtered = SearchService.FilterByDate(
            results, new DateTime(2026, 5, 1), new DateTime(2026, 6, 1));
        Assert.Empty(filtered);
    }

    [Fact]
    public void FilterByDate_AfterRange()
    {
        var results = new List<SearchResult>
        {
            new() { FilePath = "a.cs", LastModified = new DateTime(2026, 7, 1) }
        };
        var filtered = SearchService.FilterByDate(
            results, new DateTime(2026, 5, 1), new DateTime(2026, 6, 1));
        Assert.Empty(filtered);
    }

    [Fact]
    public void FilterByDate_OnlyFrom()
    {
        var results = new List<SearchResult>
        {
            new() { FilePath = "a.cs", LastModified = new DateTime(2026, 5, 15) },
            new() { FilePath = "b.cs", LastModified = new DateTime(2026, 4, 15) }
        };
        var filtered = SearchService.FilterByDate(
            results, new DateTime(2026, 5, 1), null);
        Assert.Single(filtered);
        Assert.Equal("a.cs", filtered[0].FilePath);
    }

    [Fact]
    public void FilterByDate_OnlyTo()
    {
        var results = new List<SearchResult>
        {
            new() { FilePath = "a.cs", LastModified = new DateTime(2026, 5, 15) },
            new() { FilePath = "b.cs", LastModified = new DateTime(2026, 7, 1) }
        };
        var filtered = SearchService.FilterByDate(
            results, null, new DateTime(2026, 6, 1));
        Assert.Single(filtered);
        Assert.Equal("a.cs", filtered[0].FilePath);
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

```powershell
cd tests\FastDog.Tests
dotnet test --filter "FullyQualifiedName~DateFilterTests" -v n
```

Expected: 编译失败，SearchService 类不存在

- [ ] **Step 3: 实现 SearchService**

```csharp
// src/FastDog/Services/SearchService.cs
using System.Collections.Concurrent;
using FastDog.Models;

namespace FastDog.Services;

public class SearchService
{
    private readonly RipgrepBridge _bridge = new();
    private CancellationTokenSource? _cts;

    public bool IsSearching => _cts is not null;

    public event Action<SearchResult>? ResultReceived;
    public event Action<string>? SearchCompleted;
    public event Action? SearchCancelled;

    public async Task SearchAsync(SearchQuery query, string basePath)
    {
        _cts = new CancellationTokenSource();
        try
        {
            var arguments = RipgrepBridge.BuildArguments(query);
            var fileResults = new ConcurrentDictionary<string, SearchResult>();

            await foreach (var rgEvent in _bridge.SearchAsync(arguments, _cts.Token))
            {
                switch (rgEvent.Type)
                {
                    case RgEventType.Match:
                        var result = fileResults.GetOrAdd(rgEvent.FilePath, path => new SearchResult
                        {
                            FilePath = path,
                            RelativePath = Path.GetRelativePath(basePath, path),
                            LastModified = File.GetLastWriteTime(path),
                            FileSize = new FileInfo(path).Length
                        });
                        result.Matches.Add(new MatchLine
                        {
                            LineNumber = rgEvent.LineNumber,
                            LineText = rgEvent.LineText,
                            MatchStart = rgEvent.MatchStart,
                            MatchEnd = rgEvent.MatchEnd
                        });
                        result.MatchCount = result.Matches.Count;
                        break;

                    case RgEventType.FileEnd:
                        if (fileResults.TryRemove(rgEvent.FilePath, out var fileResult))
                        {
                            // 日期过滤
                            if (query.DateFilterEnabled && !PassDateFilter(fileResult, query))
                                continue;

                            ResultReceived?.Invoke(fileResult);
                        }
                        break;

                    case RgEventType.Summary:
                        SearchCompleted?.Invoke(rgEvent.Elapsed);
                        break;
                }
            }
        }
        catch (OperationCanceledException)
        {
            SearchCancelled?.Invoke();
        }
        finally
        {
            _cts.Dispose();
            _cts = null;
        }
    }

    public void Cancel()
    {
        _cts?.Cancel();
        _bridge.KillProcess();
    }

    public static List<SearchResult> FilterByDate(
        List<SearchResult> results, DateTime? from, DateTime? to)
    {
        return results.Where(r =>
        {
            if (from.HasValue && r.LastModified < from.Value.Date) return false;
            if (to.HasValue && r.LastModified > to.Value.Date.AddDays(1)) return false;
            return true;
        }).ToList();
    }

    private static bool PassDateFilter(SearchResult result, SearchQuery query)
    {
        if (query.DateFrom.HasValue && result.LastModified < query.DateFrom.Value.Date) return false;
        if (query.DateTo.HasValue && result.LastModified > query.DateTo.Value.Date.AddDays(1)) return false;
        return true;
    }
}
```

- [ ] **Step 4: 运行测试确认通过**

```powershell
cd tests\FastDog.Tests
dotnet test --filter "FullyQualifiedName~DateFilterTests" -v n
```

Expected: 全部 PASSED

- [ ] **Step 5: 运行所有测试**

```powershell
dotnet test FastDog.sln -v n
```

Expected: 全部 PASSED

- [ ] **Step 6: 提交**

```powershell
git add src\FastDog\Services\SearchService.cs tests\FastDog.Tests\DateFilterTests.cs
git commit -m "feat: add SearchService with date filtering and search orchestration"
```

---

### Task 6: MainViewModel

**Files:**
- Create: `src/FastDog/ViewModels/MainViewModel.cs`

- [ ] **Step 1: 实现 MainViewModel**

```csharp
// src/FastDog/ViewModels/MainViewModel.cs
using System.Collections.ObjectModel;
using System.Diagnostics;
using System.Windows;
using CommunityToolkit.Mvvm.ComponentModel;
using CommunityToolkit.Mvvm.Input;
using FastDog.Models;
using FastDog.Services;

namespace FastDog.ViewModels;

public partial class MainViewModel : ObservableObject
{
    private readonly SearchService _searchService = new();

    // --- 搜索条件 ---

    [ObservableProperty] private string _searchPath = string.Empty;
    [ObservableProperty] private string _searchText = string.Empty;
    [ObservableProperty] private bool _isRegex = true;
    [ObservableProperty] private bool _isPlainText = false;
    [ObservableProperty] private bool _caseSensitive = false;
    [ObservableProperty] private bool _wholeWord = false;
    [ObservableProperty] private string _fileFilter = string.Empty;
    [ObservableProperty] private string _excludeDirs = "bin;obj";
    [ObservableProperty] private bool _dateFilterEnabled = false;
    [ObservableProperty] private DateTime? _dateFrom;
    [ObservableProperty] private DateTime? _dateTo;

    // --- 搜索结果 ---

    public ObservableCollection<SearchResult> SearchResults { get; } = [];
    public ObservableCollection<MatchLine> MatchLines { get; } = [];

    [ObservableProperty] private SearchResult? _selectedResult;
    [ObservableProperty] private MatchLine? _selectedMatchLine;

    // --- 状态栏 ---

    [ObservableProperty] private bool _isSearching = false;
    [ObservableProperty] private string _statusText = "就绪";
    [ObservableProperty] private int _totalFiles = 0;
    [ObservableProperty] private int _totalMatches = 0;
    [ObservableProperty] private string _elapsedTime = string.Empty;

    public MainViewModel()
    {
        _searchService.ResultReceived += OnResultReceived;
        _searchService.SearchCompleted += OnSearchCompleted;
        _searchService.SearchCancelled += OnSearchCancelled;
    }

    partial void OnSelectedResultChanged(SearchResult? value)
    {
        MatchLines.Clear();
        if (value is not null)
        {
            foreach (var match in value.Matches)
                MatchLines.Add(match);
        }
    }

    // --- 命令 ---

    [RelayCommand]
    private async Task SearchAsync()
    {
        if (string.IsNullOrWhiteSpace(SearchPath) || string.IsNullOrWhiteSpace(SearchText))
            return;

        if (!Directory.Exists(SearchPath))
        {
            MessageBox.Show("搜索路径不存在", "错误", MessageBoxButton.OK, MessageBoxImage.Warning);
            return;
        }

        // 清空上次结果
        SearchResults.Clear();
        MatchLines.Clear();
        TotalFiles = 0;
        TotalMatches = 0;
        IsSearching = true;
        StatusText = "搜索中...";

        var query = new SearchQuery
        {
            SearchPath = SearchPath,
            SearchText = SearchText,
            IsRegex = IsRegex,
            CaseSensitive = CaseSensitive,
            WholeWord = WholeWord,
            FileFilter = FileFilter,
            ExcludeDirs = ExcludeDirs,
            DateFilterEnabled = DateFilterEnabled,
            DateFrom = DateFrom,
            DateTo = DateTo
        };

        try
        {
            await _searchService.SearchAsync(query, SearchPath);
        }
        catch (Exception ex)
        {
            MessageBox.Show($"搜索出错: {ex.Message}", "错误", MessageBoxButton.OK, MessageBoxImage.Error);
            IsSearching = false;
            StatusText = "搜索失败";
        }
    }

    [RelayCommand]
    private void Cancel()
    {
        _searchService.Cancel();
    }

    [RelayCommand]
    private void Browse()
    {
        var dialog = new System.Windows.Forms.FolderBrowserDialog
        {
            SelectedPath = SearchPath
        };
        if (dialog.ShowDialog() == System.Windows.Forms.DialogResult.OK)
        {
            SearchPath = dialog.SelectedPath;
        }
    }

    [RelayCommand]
    private void OpenFile()
    {
        if (SelectedResult is null) return;
        try
        {
            Process.Start(new ProcessStartInfo(SelectedResult.FilePath) { UseShellExecute = true });
        }
        catch { }
    }

    [RelayCommand]
    private void OpenFileAtLine()
    {
        if (SelectedResult is null || SelectedMatchLine is null) return;
        try
        {
            // 尝试用 VS Code 打开并跳转行号
            Process.Start(new ProcessStartInfo
            {
                FileName = "code",
                Arguments = $"--goto \"{SelectedResult.FilePath}:{SelectedMatchLine.LineNumber}\"",
                UseShellExecute = true
            });
        }
        catch
        {
            // 回退：仅打开文件
            try
            {
                Process.Start(new ProcessStartInfo(SelectedResult.FilePath) { UseShellExecute = true });
            }
            catch { }
        }
    }

    [RelayCommand]
    private void CopyPath()
    {
        if (SelectedResult is null) return;
        Clipboard.SetText(SelectedResult.FilePath);
        StatusText = "已复制路径";
    }

    [RelayCommand]
    private void CopyFileName()
    {
        if (SelectedResult is null) return;
        Clipboard.SetText(SelectedResult.FileName);
        StatusText = "已复制文件名";
    }

    // --- 事件处理 ---

    private void OnResultReceived(SearchResult result)
    {
        Application.Current.Dispatcher.Invoke(() =>
        {
            SearchResults.Add(result);
            TotalFiles = SearchResults.Count;
            TotalMatches = SearchResults.Sum(r => r.MatchCount);
        });
    }

    private void OnSearchCompleted(string elapsed)
    {
        Application.Current.Dispatcher.Invoke(() =>
        {
            IsSearching = false;
            ElapsedTime = elapsed;
            StatusText = $"搜索完成 | {TotalFiles} 个文件 | {TotalMatches} 处匹配 | {elapsed}";
        });
    }

    private void OnSearchCancelled()
    {
        Application.Current.Dispatcher.Invoke(() =>
        {
            IsSearching = false;
            StatusText = $"搜索已取消 | {TotalFiles} 个文件 | {TotalMatches} 处匹配";
        });
    }
}
```

- [ ] **Step 2: 验证构建**

```powershell
dotnet build FastDog.sln
```

Expected: BUILD SUCCEEDED。注意：需要添加 `System.Windows.Forms` 引用用于 FolderBrowserDialog。

在 `FastDog.csproj` 的 `<ItemGroup>` 中添加：
```xml
<Using Include="System.Windows.Forms" />
```

并在 `<PropertyGroup>` 中添加：
```xml
<UseWindowsForms>true</UseWindowsForms>
```

- [ ] **Step 3: 提交**

```powershell
git add src\FastDog\ViewModels\MainViewModel.cs src\FastDog\FastDog.csproj
git commit -m "feat: add MainViewModel with search commands and result management"
```

---

### Task 7: UI - 搜索条件区

**Files:**
- Modify: `src/FastDog/MainWindow.xaml`
- Modify: `src/FastDog/MainWindow.xaml.cs`
- Modify: `src/FastDog/App.xaml`

- [ ] **Step 1: 配置 App.xaml**

```xml
<!-- src/FastDog/App.xaml -->
<Application x:Class="FastDog.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:FastDog.ViewModels"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <vm:MainViewModel x:Key="MainViewModel" />
    </Application.Resources>
</Application>
```

- [ ] **Step 2: 实现 MainWindow.xaml**

```xml
<!-- src/FastDog/MainWindow.xaml -->
<Window x:Class="FastDog.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:FastDog.ViewModels"
        Title="FastDog - 文本搜索" Height="650" Width="1000"
        AllowDrop="True" DragOver="Window_DragOver" Drop="Window_Drop"
        DataContext="{StaticResource MainViewModel}">
    <DockPanel>
        <!-- 搜索条件区 -->
        <Border DockPanel.Dock="Top" BorderBrush="#DDD" BorderThickness="0,0,0,1"
                Padding="10,8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Row 0: 搜索路径 -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="搜索路径:"
                           VerticalAlignment="Center" Margin="0,0,6,0"/>
                <TextBox Grid.Row="0" Grid.Column="1"
                         Text="{Binding SearchPath, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,2"/>
                <Button Grid.Row="0" Grid.Column="2" Content="浏览..."
                        Command="{Binding BrowseCommand}" Margin="6,2,0,2" Padding="12,2"/>

                <!-- Row 1: 搜索内容 + 选项 -->
                <TextBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,4,0,0"/>
                <StackPanel Grid.Row="1" Grid.Column="2" Orientation="Horizontal"
                            Margin="6,4,0,0">
                    <RadioButton Content="正则" IsChecked="{Binding IsRegex}"
                                 Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <RadioButton Content="文本" IsChecked="{Binding IsPlainText}"
                                 Margin="0,0,12,0" VerticalAlignment="Center"/>
                    <CheckBox Content="区分大小写" IsChecked="{Binding CaseSensitive}"
                              Margin="0,0,12,0" VerticalAlignment="Center"/>
                    <CheckBox Content="全词" IsChecked="{Binding WholeWord}"
                              VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Row 2: 文件过滤 + 排除目录 -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="文件过滤:"
                           VerticalAlignment="Center" Margin="0,4,6,0"/>
                <TextBox Grid.Row="2" Grid.Column="1"
                         Text="{Binding FileFilter, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,4,0,0"/>
                <StackPanel Grid.Row="2" Grid.Column="2" Orientation="Horizontal"
                            Margin="6,4,0,0">
                    <TextBlock Text="排除目录:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                    <TextBox Text="{Binding ExcludeDirs, UpdateSourceTrigger=PropertyChanged}"
                             Width="180"/>
                </StackPanel>

                <!-- Row 3: 日期 + 按钮 -->
                <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                            Orientation="Horizontal" Margin="0,6,0,0">
                    <CheckBox Content="修改日期:" IsChecked="{Binding DateFilterEnabled}"
                              VerticalAlignment="Center" Margin="0,0,4,0"/>
                    <DatePicker SelectedDate="{Binding DateFrom}" Width="120"
                                IsEnabled="{Binding DateFilterEnabled}"/>
                    <TextBlock Text="~" VerticalAlignment="Center" Margin="4,0"/>
                    <DatePicker SelectedDate="{Binding DateTo}" Width="120"
                                IsEnabled="{Binding DateFilterEnabled}"/>
                </StackPanel>
                <StackPanel Grid.Row="3" Grid.Column="2" Orientation="Horizontal"
                            HorizontalAlignment="Right" Margin="6,6,0,0">
                    <Button Content="搜索" Command="{Binding SearchCommand}"
                            Padding="20,4" IsDefault="True"/>
                    <Button Content="取消" Command="{Binding CancelCommand}"
                            Padding="12,4" Margin="6,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 状态栏 -->
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}"/>
            </StatusBarItem>
        </StatusBar>

        <!-- 中间：结果区 -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 文件列表 -->
            <DataGrid Grid.Row="0"
                      ItemsSource="{Binding SearchResults}"
                      SelectedItem="{Binding SelectedResult}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      MouseDoubleClick="DataGrid_MouseDoubleClick">
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="打开文件" Command="{Binding OpenFileCommand}"/>
                        <MenuItem Header="复制路径" Command="{Binding CopyPathCommand}"/>
                        <MenuItem Header="复制文件名" Command="{Binding CopyFileNameCommand}"/>
                    </ContextMenu>
                </DataGrid.ContextMenu>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="文件名" Binding="{Binding FileName}" Width="200"/>
                    <DataGridTextColumn Header="大小" Binding="{Binding FileSize, StringFormat='{}{0:N0}'}" Width="80"/>
                    <DataGridTextColumn Header="匹配数" Binding="{Binding MatchCount}" Width="60"/>
                    <DataGridTextColumn Header="路径" Binding="{Binding RelativePath}" Width="*"/>
                    <DataGridTextColumn Header="修改时间" Binding="{Binding LastModified, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="140"/>
                </DataGrid.Columns>
            </DataGrid>

            <GridSplitter Grid.Row="1" Height="4" HorizontalAlignment="Stretch"/>

            <!-- 行内容预览 -->
            <ListBox Grid.Row="2"
                     ItemsSource="{Binding MatchLines}"
                     SelectedItem="{Binding SelectedMatchLine}"
                     MouseDoubleClick="MatchList_MouseDoubleClick">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding LineText}" TextTrimming="None"/>
                    </TextBlock>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </DockPanel>
</Window>
```

- [ ] **Step 3: 实现 MainWindow.xaml.cs**

```csharp
// src/FastDog/MainWindow.xaml.cs
using System.Windows;
using FastDog.ViewModels;

namespace FastDog;

public partial class MainWindow : Window
{
    public MainWindow()
    {
        InitializeComponent();
    }

    private void DataGrid_MouseDoubleClick(object sender, System.Windows.Input.MouseButtonEventArgs e)
    {
        var vm = (MainViewModel)DataContext;
        vm.OpenFileCommand.Execute(null);
    }

    private void MatchList_MouseDoubleClick(object sender, System.Windows.Input.MouseButtonEventArgs e)
    {
        var vm = (MainViewModel)DataContext;
        vm.OpenFileAtLineCommand.Execute(null);
    }

    private void Window_DragOver(object sender, DragEventArgs e)
    {
        e.Effects = e.Data.GetDataPresent(DataFormats.FileDrop)
            ? DragDropEffects.Copy
            : DragDropEffects.None;
        e.Handled = true;
    }

    private void Window_Drop(object sender, DragEventArgs e)
    {
        if (e.Data.GetDataPresent(DataFormats.FileDrop))
        {
            var files = (string[])e.Data.GetData(DataFormats.FileDrop);
            if (files.Length > 0 && Directory.Exists(files[0]))
            {
                var vm = (MainViewModel)DataContext;
                vm.SearchPath = files[0];
            }
        }
    }
}
```

- [ ] **Step 4: 更新 App.xaml.cs（如需要）**

确保 `App.xaml.cs` 不包含多余的启动逻辑：

```csharp
// src/FastDog/App.xaml.cs
using System.Windows;

namespace FastDog;

public partial class App : Application
{
}
```

- [ ] **Step 5: 构建并运行验证**

```powershell
dotnet build FastDog.sln
dotnet run --project src\FastDog
```

Expected: 窗口正常显示，搜索条件区和结果区布局正确。输入路径和搜索文本，点击搜索按钮能触发搜索。

- [ ] **Step 6: 提交**

```powershell
git add src\FastDog\MainWindow.xaml src\FastDog\MainWindow.xaml.cs src\FastDog\App.xaml src\FastDog\App.xaml.cs
git commit -m "feat: implement MainWindow UI with search conditions and results layout"
```

---

### Task 8: UI 完善 - 行内容高亮 + 状态栏增强

**Files:**
- Modify: `src/FastDog/MainWindow.xaml`

- [ ] **Step 1: 添加匹配行高亮的 ValueConverter**

创建 `src/FastDog/Converters/MatchHighlightConverter.cs`：

```csharp
// src/FastDog/Converters/MatchHighlightConverter.cs
using System.Globalization;
using System.Windows;
using System.Windows.Data;
using System.Windows.Documents;
using FastDog.Models;

namespace FastDog.Converters;

public class MatchLineToInlinesConverter : IValueConverter
{
    public object Convert(object value, Type targetType, object parameter, CultureInfo culture)
    {
        if (value is not MatchLine match) return new Run();

        var inlines = new List<Inline>
        {
            new Run($"{match.LineNumber}: ") { FontWeight = FontWeights.Bold, Foreground = SystemColors.GrayTextBrush }
        };

        // 高亮匹配部分
        var text = match.LineText.TrimEnd('\r', '\n');
        if (match.MatchStart > 0)
            inlines.Add(new Run(text[..match.MatchStart]));

        if (match.MatchEnd <= text.Length && match.MatchStart < match.MatchEnd)
        {
            inlines.Add(new Run(text[match.MatchStart..match.MatchEnd])
            {
                Background = new System.Windows.Media.SolidColorBrush(
                    System.Windows.Media.Colors.LightYellow),
                FontWeight = FontWeights.Bold
            });
        }

        if (match.MatchEnd < text.Length)
            inlines.Add(new Run(text[match.MatchEnd..]));

        return inlines;
    }

    public object ConvertBack(object value, Type targetType, object parameter, CultureInfo culture)
        => throw new NotSupportedException();
}
```

- [ ] **Step 2: 更新 MainWindow.xaml 中的 ListBox 使用高亮**

在 `MainWindow.xaml` 中添加 converter 引用并更新 ListBox：

```xml
<!-- 在 Window 标签内添加 xmlns -->
xmlns:converters="clr-namespace:FastDog.Converters"

<!-- 在 Window.Resources 中添加 -->
<Window.Resources>
    <converters:MatchLineToInlinesConverter x:Key="MatchLineConverter"/>
</Window.Resources>

<!-- 替换 ListBox.ItemTemplate -->
<ListBox.ItemTemplate>
    <DataTemplate>
        <TextBlock TextTrimming="CharacterEllipsis">
            <TextBlock.Inlines>
                <MultiBinding Converter="{StaticResource MatchLineConverter}">
                    <Binding />
                </MultiBinding>
            </TextBlock.Inlines>
        </TextBlock>
    </DataTemplate>
</ListBox.ItemTemplate>
```

注意：Inlines 不能直接通过 Binding 设置。改用更简单的方式 —— 用 `Loaded` 事件或直接显示带行号前缀的文本，高亮后续手动完善。实际上最简单的方式是直接显示文本：

```xml
<!-- 简化方案：直接显示文本 -->
<ListBox.ItemTemplate>
    <DataTemplate>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="{Binding LineNumber}" Width="50"
                       Foreground="Gray" FontWeight="Bold"
                       VerticalAlignment="Center"/>
            <TextBlock Text="{Binding LineText}" TextTrimming="CharacterEllipsis"/>
        </StackPanel>
    </DataTemplate>
</ListBox.ItemTemplate>
```

- [ ] **Step 3: 增强状态栏**

更新 StatusBar 部分：

```xml
<StatusBar DockPanel.Dock="Bottom">
    <StatusBarItem>
        <TextBlock Text="{Binding StatusText}"/>
    </StatusBarItem>
    <Separator/>
    <StatusBarItem HorizontalAlignment="Right">
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="文件: "/>
            <TextBlock Text="{Binding TotalFiles, Mode=OneWay}" FontWeight="Bold"/>
            <TextBlock Text=" | 匹配: " Margin="8,0,0,0"/>
            <TextBlock Text="{Binding TotalMatches, Mode=OneWay}" FontWeight="Bold"/>
            <TextBlock Text="{Binding ElapsedTime, StringFormat=' | {0}'}" Margin="8,0,0,0"/>
        </StackPanel>
    </StatusBarItem>
</StatusBar>
```

- [ ] **Step 4: 构建并运行验证**

```powershell
dotnet build FastDog.sln
dotnet run --project src\FastDog
```

Expected: 搜索结果中文件列表正常显示，点击文件后下方显示匹配行（带行号），状态栏显示统计信息。

- [ ] **Step 5: 提交**

```powershell
git add -A
git commit -m "feat: add match line display with line numbers and enhanced status bar"
```

---

### Task 9: 集成验证 + 收尾

**Files:**
- Modify: `src/FastDog/MainWindow.xaml` (标题、图标等)
- Modify: `src/FastDog/FastDog.csproj` (发布配置)

- [ ] **Step 1: 端到端手动测试**

运行应用并测试以下场景：
1. 搜索路径指向 `E:\demo\ai\my\fastdog\grepWin\src`
2. 搜索文本 `class`，正则模式，不区分大小写
3. 验证结果文件列表正确显示
4. 点击文件，验证匹配行预览正确
5. 双击文件，验证能打开
6. 测试文件过滤（如 `*.cpp`）
7. 测试排除目录
8. 测试取消按钮
9. 测试拖放文件夹到窗口
10. 测试右键菜单复制路径

- [ ] **Step 2: 修复发现的问题**

根据手动测试结果修复 bug。

- [ ] **Step 3: 运行全部测试**

```powershell
dotnet test FastDog.sln -v n
```

Expected: 全部 PASSED

- [ ] **Step 4: 最终提交**

```powershell
git add -A
git commit -m "feat: complete FastDog v1 - text search tool with ripgrep backend"
```

---

## 自检结果

**1. Spec 覆盖率检查：**
- ✅ 纯文本搜索 → Task 3 (BuildArguments, `-F` 参数)
- ✅ 正则表达式搜索 → Task 3 (BuildArguments, 默认行为)
- ✅ 大小写敏感 → Task 3 (`-i` 参数)
- ✅ 全词匹配 → Task 3 (`-w` 参数)
- ✅ 文件名过滤 → Task 3 (`--iglob` 参数)
- ✅ 排除目录 → Task 3 (`--glob !dir` 参数)
- ✅ 日期范围过滤 → Task 5 (FilterByDate)
- ✅ 文件列表 → Task 7 (DataGrid)
- ✅ 行内容预览 → Task 7/8 (ListBox)
- ✅ 双击打开 → Task 7 (MouseDoubleClick)
- ✅ 复制路径 → Task 6 (CopyPathCommand)
- ✅ 跳转行号 → Task 6 (OpenFileAtLineCommand)
- ✅ 取消按钮 → Task 6 (CancelCommand)
- ✅ 拖入文件夹 → Task 7 (Window_Drop)
- ✅ 状态栏 → Task 7/8 (StatusBar)
- ✅ rg.exe 捆绑 → Task 1 (tools/ 目录) + Task 4 (FindRgPath)

**2. Placeholder 扫描：** 无 TBD/TODO/占位符。

**3. 类型一致性：** 所有类型定义在使用前均已创建。`SearchResult`、`MatchLine`、`SearchQuery`、`RgEvent` 在各 Task 间引用一致。
